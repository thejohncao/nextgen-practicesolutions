
export interface Phase {
  title: string;
  agent: string;
  story: string;
  color: string;
  borderColor: string;
  textColor: string;
}

// Note: Phases data has been moved to TeamPhases.ts
export const phases: Phase[] = [];
